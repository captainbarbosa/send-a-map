# Uncomment the next line to define a global platform for your project

platform :ios, '9.0'
use_frameworks!


def shared_pods
  pod 'Mapbox-iOS-SDK'
  pod 'MapboxStatic.swift', '~> 0.10'
end

target 'send-a-map' do
  shared_pods
end


target 'send-a-map-Tests' do
  shared_pods
end
